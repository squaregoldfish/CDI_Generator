The SOCATv3 importer was only used for initial testing,
so there may not be enough models to cover all the different
formats in SOCATv3.

We should only be using SOCATv4 anyway, so this shouldn't matter.
